# EFHW 馈电方式对比试验报告
## 49:1 变压器（3×FT-240-51） vs LC 调谐器 @ 21 MHz

| 项目 | 内容 |
|------|------|
| 电台 | BG1SB（ON80da，北京地区） |
| 频率 / 模式 | 21.074 MHz（15m）FT8 |
| 发射功率 | 100 W |
| 试验日期 | 2026-06-19 |
| 数据来源 | PSK Reporter 24h ADIF 接口（`pskdata.pl?adif=1&days=1`） |
| 切换时刻 | 2026-06-19 **12:26:52 CST**（A→B） |
| 文档版本 | v1（阶段性，单次顺序测） |

---

## 1. 试验目的

比较同一副 EFHW（半波端馈）天线在两种馈电匹配方式下的实际链路性能：

- **配置 A**：49:1 阻抗变压器，磁芯为 **3 × FT-240-51** 堆叠，不接调谐器。
- **配置 B**：LC 调谐器（L 网络）匹配。

核心问题：**换用 LC 调谐器，能否在 21 MHz 上带来可测量的天线效率/链路增益？**

---

## 2. 试验配置与实测电气参数

| 参数 | 配置 A（49:1） | 配置 B（LC 调谐器） |
|------|------|------|
| 匹配方式 | 49:1 变压器（3×FT-240-51） | LC 网络（L-match） |
| 21.074 MHz SWR | **1.47** | **1.30** |
| 失配损耗（由 SWR 计算）| 0.16 dB | 0.075 dB |
| 功率 | 100 W | 100 W |
| 工作时段（CST） | 11:35–12:26（取尾段 51 min） | 12:26:52–13:17（51 min） |

**失配损耗差异 = 0.16 − 0.075 ≈ 0.09 dB** —— 电气上可忽略。
> SWR→失配损耗：反射系数 Γ=(SWR−1)/(SWR+1)，失配损耗 = −10·log₁₀(1−Γ²)。
> SWR 1.47 → Γ=0.190 → 3.6% 反射 → 0.16 dB；SWR 1.30 → Γ=0.130 → 1.7% 反射 → 0.075 dB。

---

## 3. 数据采集方法（演进过程）

试验中采集方法经历了三次迭代，记录在此以备复现与审查：

### 3.1 实时查询接口（已弃用）
- `retrieve.pskreporter.info/query?senderCallsign=bg1sb&flowStartSeconds=-900`
- 返回最近 900 秒滚动窗口 XML。
- **问题**：① 严格限流，间隔 <10 min 频繁返回 HTTP 503；② 900 秒滚动窗口导致 A/B 切换后旧数据残留约 15 分钟；③ PSK Reporter 收报有 5–8 分钟上报延迟。
- 脚本：`grab_pskr.py`（窗口快照）、`grab_raw.py`（累积去重，后台循环，因限流基本失败）。

### 3.2 24h ADIF 批量接口（最终采用）
- `www.pskreporter.info/cgi-bin/pskdata.pl?adif=1&days=1&callsign=<call>`
- 一次返回过去 24h 全部收报，**带精确 UTC 时间戳**，无滚动窗口、无限流问题。
- 可按切换时刻 12:26:52 精确切片成 A/B 两段。
- 脚本：`adif_compare.py`、`cross_full.py`、`paired_snr.py`、`paired_snr2.py`。

### 3.3 同栅格探针法（传播扣除）
- 额外下载 ON80 网格内其他电台的 ADIF（谁收到它们），作为**天线未改变的传播基准**。
- 候选探针：BH1UWJ、BD1AUJ、BI1WIA、BI1MDW、BI1KND。
- 有效探针（A/B 两窗均活跃）：**BH1UWJ、BI1WIA**（其余在某一窗口无 15m 数据）。

### 关键数据文件（均可追溯，未提交 git）
| 文件 | 内容 |
|------|------|
| `pskr_24h_latest.adi`（153 KB，13:18 抓取） | BG1SB 24h 收报，主分析输入 |
| `pskr_bg1sb_full.adi`（176 KB，13:29 抓取） | BG1SB 最新全量 |
| `probe_*.adi` | 5 个 ON80 探针台 24h 收报 |
| `efhw_test_log.json` | 时间线、SWR、切换标记 |
| `pskr_loop.log` | 后台采集循环日志（含 503 记录） |

---

## 4. 分析与结果

分析分三层推进，**后一层修正前一层**，完整保留以展示推理链。

### 4.1 第一层：收报数量对比（❌ 已否定）

等长窗口（各 51 分钟）：

| 指标 | A（49:1） | B（LC） | B/A |
|------|------|------|------|
| 收报数 | 207 | 270 | 1.30 |
| 独立台站 | 131 | 157 | 1.20 |
| 覆盖国家 | 9 | 15 | 1.67 |
| 平均距离 | 3078 km | 3622 km | — |

引入探针扣传播后曾得出"**LC 强约 2 倍 / +2.6 dB**"。

**❌ 此结论被否定**，原因：
1. **收报数量受发射占空比污染** —— A、B 两段实际发射的 FT8 次数不同，"收到次数"无法反映天线增益。
2. **少数近距台反复解码刷量** —— B 段大量计数来自极少数中国台多次解码。
3. FT8 解码门限的选择效应：边缘台从"解不出"到"刚好解出"会虚增计数。

### 4.2 第二层：同呼号配对 ΔSNR（✅ 正确口径）

**方法**（由操作员提出，是本试验方法论的转折点）：
> 对**在 A、B 两段都报告过 BG1SB 的同一接收呼号**，计算各自 ΔSNR = mean(SNR_B) − mean(SNR_A)。每个台用自身作对照，**路径、距离、接收机灵敏度全部抵消**。再用同栅格探针的同口径 ΔSNR 扣除电离层漂移。两级配对，最干净。

**BG1SB（被测，换了天线）：**

| 统计量 | paired_snr2（主线） | ab-snr（独立分叉，交叉验证） |
|------|------|------|
| 共同呼号 n | 80 | 90 |
| ΔSNR 均值 | −0.52 dB | −0.17 dB |
| ΔSNR 中位数 | **0.00 dB** | **0.00 dB** |
| 标准差 | 5.48 dB | 5.56 dB |
| 均值 95% CI | [−1.71, +0.75] | — |

**ON80 探针（天线未动 = 传播基准）：**

| 探针 | 共同 RX n | ΔSNR 均值 | ΔSNR 中位 |
|------|------|------|------|
| BH1UWJ | 60 | +0.75 | +1.00 |
| BI1WIA | 18 | −1.78 | −0.25 |
| **合并基准** | 78 | +0.17 | +0.75 |

**传播修正后的天线净效应（DUT − 探针基准）：**

| 口径 | 主线 | 分叉 | 是否跨 0 |
|------|------|------|------|
| 按均值 | −0.69 dB | −0.34 dB | ✅ 跨 0 [−2.25, +0.88] |
| 按中位数 | −0.75 dB | −0.75 dB | ✅ 跨 0 |
| 按加权均值 | −0.49 dB | — | ✅ 跨 0 |

> 正值 = LC 调谐器在扣除传播后改善了 SNR。实测全部为小幅负值或跨 0。

### 4.3 第三层：鲁棒性过滤（样本量诊断）

为剔除单次报告噪声（FT8 单次 SNR 自带 ±2~3 dB 抖动），按"每窗最少报告次数"过滤：

| 过滤门限 | DUT 共同呼号 n | 天线效应（中位） | 评价 |
|------|------|------|------|
| ≥1 次 | 80 | −0.75 dB | 样本足，单次噪声大 |
| ≥2 次 | **3** | +1.00 dB（CI [−1,+3]）| 样本崩塌，不可信 |
| ≥3 次 | 1 | — | 无法分析 |

**关键发现**：要求"同台两窗各≥2 次报告"时，共同呼号从 80 塌到 3。说明 51 分钟窗口内绝大多数台只解码到 BG1SB 一两次 —— 去噪与统计量不可兼得，这是**单次顺序测 + 短窗口的固有缺陷**。

---

## 5. 阶段性结论

> **以现有数据，无法证明 LC 调谐器与 49:1 变压器（3×FT-240-51）在 21 MHz 上存在 SNR 性能差异。最佳估计为两者实质相当，净差异落在 ±1 dB 测量噪声以内，统计上不可区分。**

支撑证据：
1. **两条独立分析路径收敛**：主线（n=80）与独立分叉（n=90）在 ΔSNR 中位数（均为 0.00）、探针基准（+0.75）、净效应区间（−0.3~−0.75 dB）上完全一致。
2. **所有可信口径都跨越 0**：均值、中位数、加权均值的置信区间无一排除"零差异"。
3. **与物理预期自洽**：失配损耗差仅 0.09 dB；3×FT-240-51 三磁环堆叠将 51 号料在 15m 的磁通密度与铁损压低，本就不该产生可测量差距。

推理链的诚实记录：
- 最早（窗口快照）判断"两者相当" → 中段（收报数量）误判"LC 强 +2.6 dB" → 操作员指出"数量无意义、应看同呼号 SNR" → 配对 ΔSNR 回到"无显著差异"。
- **数量比值是被发射占空比污染的假象；配对 ΔSNR 才是正确口径。**

---

## 6. 试验局限（必须诚实标注）

| # | 局限 | 影响 |
|---|------|------|
| 1 | **顺序测，非同时** | A、B 先后进行，电离层逐分钟变化。探针扣除大幅降低但未归零此污染。 |
| 2 | **单次报告噪声地板高** | 多数共同台 A/B 各仅 1~2 次报告，单台 ΔSNR 抖动 ±3 dB（QSB + FT8 解码抖动）；靠 n≈80–90 聚合才有意义。 |
| 3 | **有效探针仅 2 个** | BH1UWJ 与 BI1WIA 方向相反（+0.75 / −1.78），基准本身 ±1 dB 不确定。理想需 4~5 个全程活跃探针。 |
| 4 | **发射占空比不可控** | A、B 两段实际 TX 次数未记录，导致数量类指标完全不可用。 |
| 5 | **未测磁环温度** | 变压器铁损最直接的物理证据缺失。 |
| 6 | **单频段单次** | 仅 21 MHz、仅一次。变压器损耗随频率上升，结论不可外推到 10m/6m。 |
| 7 | **噪声地板限制分辨率** | 单次顺序测的方法学下限约 ±1~2 dB，无法分辨更小的真实差异。 |

---

## 7. 下一步试验建议（按性价比排序）

### 建议 1：磁环温升实测 ⭐ 强烈推荐（5 分钟，零传播噪声）
- **方法**：21.074 MHz、100 W、连续发报 10 分钟，结束立即用手或红外测温枪测 3×FT-240-51 表面温度，与环境温度对比。
- **判据**：温升 < 5–10 ℃ → 铁损可忽略，直接物理坐实"49:1 在 15m 上效率良好"；明显发烫（>20 ℃） → 存在不可忽略铁损，是 LC 可能占优的物理依据。
- **价值**：唯一**不受任何传播噪声干扰**的单点测量，可独立验证全部 SNR 结论。

### 建议 2：A/B/A/B 交替快切 ⭐ 钉死结论的正解
- **方法**：A 发 3–4 分钟 → 切 B 3–4 分钟 → 切回 A → …，重复 6–8 轮，全程记录每次切换的准确时刻。
- **分析**：沿用本报告的同呼号 ΔSNR + 探针扣除法，但**逐轮累计每个共同呼号的多次配对**，用 √N 压低单次 ±3 dB 噪声。
- **预期**：传播漂移在相邻轮次间抵消，可将分辨率从 ±1 dB 提升到约 ±0.5 dB。若 B 仍系统性高出基准 ≥+0.5 dB，方可判定真实天线增益。
- **代价**：工作量大，需配合频繁手动切换；结论很可能仍是"无显著差异"。

### 建议 3：补足探针数量
- 下一次测试前，先用 24h ADIF 扫描当时段 ON80（及邻近 OM89/ON70 等）网格中**全程活跃**的 15m 发射台，挑选 4~5 个稳定探针，提升传播基准可信度。

### 建议 4：多频段扩展
- 在 14 MHz、18 MHz、21 MHz、24 MHz、28 MHz 上分别做同样对比。
- **科学假设**：若 49:1 变压器存在频率相关铁损，则频率越高、LC 相对优势越明显。21 MHz 无差异不代表 28 MHz 无差异。这是最有物理意义的扩展方向。

### 建议 5：发射占空比对齐
- 后续测试固定每段发射 N 个 FT8 周期（如各 20 次），使数量类指标也恢复可比性，作为 ΔSNR 的旁证。

---

## 8. 附录：可复现脚本清单

| 脚本 | 作用 |
|------|------|
| `adif_compare.py` | 单台 ADIF 按切换点切 A/B，等长窗口对比（数量+SNR） |
| `cross_full.py` | 全量交叉：DUT vs 多探针 B/A 比值，传播归一化（含 UTC→CST 修复） |
| `paired_snr.py` | 同呼号配对 ΔSNR（核心方法，分叉版） |
| `paired_snr2.py` | 配对 ΔSNR + 最小报告数过滤 + bootstrap CI（主线版） |
| `grab_pskr.py` / `grab_raw.py` | 实时窗口采集（已弃用，留作记录） |

**重跑命令**：
```bash
cd /Users/cheenle/pskreporter/efhw-knowledge
python3 paired_snr2.py    # 主结论
python3 cross_full.py     # 数量交叉（已否定的口径，留作对照）
```

---

*报告生成：2026-06-19 ｜ 数据窗口：11:35–13:17 CST ｜ 方法：PSK Reporter 24h ADIF + 同栅格探针配对 ΔSNR*
